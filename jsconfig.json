// 该文件配置是对文件所在目录下的所有js作出个个性化配置，神奇的是这个文件可以让vscode设别自定义的包路径，即使什么都不添加
{
    "exclude": [ // vscode扫面文件变慢可以通过这个排序不需要扫描的文件
        "node_modules",
    ],
    "include": [
    ],
    "compilerOptions": { // webpack配置的别名，vs不能根据文件路径别名跳转到对应的文件
        "baseUrl": ".",
        "paths": {
            "@pkg/modules": ["./packages/modules"]
        }
    }
}